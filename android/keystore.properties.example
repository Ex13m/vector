# Copy to <repo>/android/keystore.properties and fill in (the real file is gitignored).
# Paths on Windows use forward slashes.
storeFile=C:/Users/User/vektor-keys/vektor-release.jks
storePassword=CHANGE_ME
keyAlias=vektor
keyPassword=CHANGE_ME
